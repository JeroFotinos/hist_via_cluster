[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "hist-via-cluster"
version = "0.0.1"
description = "Histological Analysis via µXRF"

[tool.setuptools.packages.find]
where = ["src"]
